// Gets the last element of array.

const arrayA = ['a', 'b', 'c'];
const arrayB = ['10', '9', '8'];

function last(arr) {
  return arr[arr.length - 1]
}

console.log(last(arrayA));
console.log(last(arrayB));
